<%= form_for(@global_message, :remote => true, 
             :data => {'ks-insert-response' => true}) do |f| %>
  <% if @global_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@global_message.errors.count, "error") %> prohibited this global_message from being saved:</h2>

      <ul>
      <% @global_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :jp_text %><br />
    <%= f.text_area :jp_text, :rows => 10 %>
  </div>
  <div class="field">
    <%= f.label :en_text %><br />
    <%= f.text_area :en_text, :rows => 10 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
