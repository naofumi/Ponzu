<%= form_for(@meet_up_comment) do |f| %>
  <% if @meet_up_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meet_up_comment.errors.count, "error") %> prohibited this meet_up_comment from being saved:</h2>

      <ul>
      <% @meet_up_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :meet_up_id %><br />
    <%= f.number_field :meet_up_id %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
