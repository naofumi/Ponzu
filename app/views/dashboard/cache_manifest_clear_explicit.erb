CACHE MANIFEST
# blank cache manifest for non-logged in users
# Version: <%= (Rails.env == "production") ? File.dirname(__FILE__) : Time.now %>
# <%= render :partial => "manifest_version_snippet" %>

CACHE:
NETWORK:
*

FALLBACK:

